*.iml
.kotlin
.gradle
**/build/
local.properties
.idea
.DS_Store
.claude/worktrees/
.claude/*.lock
.claude/settings.local.json

# Session-local scratch (see /next, /donext, /goodnight) — never committed.
# These are hand-off notes between Claude sessions, not documentation: they are
# accurate for hours, name in-flight PRs and worker ids, and go stale silently.
# Repo history is the wrong home for them. See CLAUDE.md, "Session scratch".
next-plan.md
next-plan-*.md
morning-report.md
morning-report-*.md

# Brainstorming visual companion sessions
.superpowers/
.jqwik-database

# Device logs pulled by spike/collect-logs.sh — evidence from a hardware run, not source
spike-logs/

# Xcode per-user data (spike/ Xcode project)
**/xcuserdata/
*.xcuserstate

# Local build/gate logs (`./gradlew … > foo.log`) — evidence from a verification run, not source
*.log
